### Prerequisites  
- Before starting, ensure you have `Node.js` installed on your system.  
- If not installed, download it from the official website: [Download Node.js](https://nodejs.org)  
- Check if Node.js is installed:
```
node -v
```
### Installation
```
cd backend  
```
```
npx @nestjs/cli new api
  ```
select `npm`
