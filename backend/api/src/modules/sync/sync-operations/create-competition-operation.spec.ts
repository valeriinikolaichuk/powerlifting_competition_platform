import { CreateCompetitionOperation } from './create-competition-operation';

describe('CreateCompetitionOperation', () => {
  it('should be defined', () => {
    expect(new CreateCompetitionOperation()).toBeDefined();
  });
});
