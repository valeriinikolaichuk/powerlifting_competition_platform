import { OfflineSessionPolicy } from './offline-session-policy';

describe('OfflineSessionPolicy', () => {
  it('should be defined', () => {
    expect(new OfflineSessionPolicy()).toBeDefined();
  });
});
